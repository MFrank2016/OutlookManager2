[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    benchmark: environment-sensitive performance benchmark
    live_server: requires a running local API/web server
    live_network: requires real external credentials or network access
