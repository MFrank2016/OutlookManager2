
## 🖥️ Frontend (Modern UI)

The project now includes a modern React-based frontend located in the `frontend` directory, built with Next.js, TypeScript, and Shadcn/ui.

### 🛠️ Development

1. **Prerequisites**: Ensure Node.js 18+ is installed.

2. **Setup**:
   ```bash
   cd frontend
   npm install
   ```

3. **Run Development Server**:
   ```bash
   npm run dev
   ```
   
   - Access the new frontend at: http://localhost:3000
   - Ensure the backend API is running at: http://localhost:8000

### ✨ Frontend Features
- **Architecture**: Next.js 14 (App Router), TypeScript, Tailwind CSS
- **UI Components**: Shadcn/ui, Lucide Icons
- **State Management**: Zustand (Auth), TanStack Query (Server State)
- **Form Handling**: React Hook Form + Zod
- **Routing**: File-based routing with protected routes
