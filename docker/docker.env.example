# Outlook邮件API服务 - Docker环境变量配置示例
# 复制此文件为 .env 并根据需要修改配置
#
# 使用方法：
# 1. 复制此文件：cp docker/docker.env.example .env
# 2. 根据需要修改配置值
# 3. 使用 docker-compose up -d 启动服务

# =============================================================================
# 服务配置
# =============================================================================

# 服务监听地址（默认: 0.0.0.0，监听所有网络接口）
HOST=0.0.0.0

# 服务监听端口（默认: 8000，容器内部端口）
# 注意：docker-compose.yml 中的端口映射为 8001:8000
# 即外部访问使用 8001 端口，容器内部使用 8000 端口
PORT=8000

# =============================================================================
# Python运行时配置
# =============================================================================

# 禁用Python字节码文件生成（默认: 1）
PYTHONDONTWRITEBYTECODE=1

# 强制Python输出不缓冲（默认: 1）
# 这对于Docker日志很重要，确保日志实时输出
PYTHONUNBUFFERED=1

# =============================================================================
# 应用配置
# =============================================================================

# 日志级别（可选: debug, info, warning, error）
# 注意：uvicorn 使用小写日志级别
LOG_LEVEL=info

# 时区设置（默认: Asia/Shanghai）
TZ=Asia/Shanghai

# =============================================================================
# 数据库配置
# =============================================================================

# SQLite数据库文件路径（默认: /app/data.db）
# 通过 docker-compose.yml 中的 volume 挂载到宿主机
# DB_PATH=/app/data.db

# =============================================================================
# 缓存配置
# =============================================================================

# 邮件列表缓存大小（默认: 1000）
# EMAIL_LIST_CACHE_SIZE=1000

# 邮件详情缓存大小（默认: 500）
# EMAIL_DETAIL_CACHE_SIZE=500

# Access Token缓存大小（默认: 200）
# ACCESS_TOKEN_CACHE_SIZE=200

# =============================================================================
# 高级配置（通常无需修改）
# =============================================================================

# IMAP服务器（默认: outlook.live.com）
# IMAP_SERVER=outlook.live.com

# IMAP端口（默认: 993）
# IMAP_PORT=993

# Graph API基础URL（默认: https://graph.microsoft.com/v1.0）
# GRAPH_API_BASE_URL=https://graph.microsoft.com/v1.0 