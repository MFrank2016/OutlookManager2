# Outlook邮件API服务 - 环境变量配置示例
# 复制此文件为 .env 并根据需要修改配置
#
# 使用方法：
# 1. 复制此文件：cp env.example .env
# 2. 根据需要修改配置值
# 3. 使用 python main.py 启动服务

# =============================================================================
# 应用配置
# =============================================================================

# 服务监听地址（默认: 0.0.0.0，监听所有网络接口）
HOST=0.0.0.0

# 服务监听端口（默认: 8000）
PORT=8000

# 日志级别（可选: debug, info, warning, error）
LOG_LEVEL=info

# 时区设置（默认: Asia/Shanghai）
TZ=Asia/Shanghai

# =============================================================================
# 数据库配置
# =============================================================================

# 数据库类型：'sqlite' 或 'postgresql'
# 默认: sqlite
DB_TYPE=postgresql

# PostgreSQL连接配置（当DB_TYPE=postgresql时使用）
# 本地开发连接远程PostgreSQL时，使用远程服务器的IP地址
DB_HOST=192.168.1.100          # 远程PostgreSQL服务器IP地址
DB_PORT=5432                    # PostgreSQL端口
DB_NAME=outlook_manager         # 数据库名
DB_USER=outlook_user           # 数据库用户名
DB_PASSWORD=your_password_here # 数据库密码（请修改为实际密码）

# PostgreSQL连接池配置
DB_POOL_SIZE=5                 # 最小连接数
DB_MAX_OVERFLOW=15             # 最大连接数 = POOL_SIZE + MAX_OVERFLOW
DB_POOL_TIMEOUT=30             # 连接超时（秒）

# SQLite数据库文件路径（当DB_TYPE=sqlite时使用）
# DB_FILE=data.db

# =============================================================================
# 使用说明
# =============================================================================
#
# 1. 连接远程PostgreSQL：
#    - 设置 DB_TYPE=postgresql
#    - 设置 DB_HOST 为远程服务器IP（如：192.168.1.100）
#    - 设置 DB_PORT、DB_NAME、DB_USER、DB_PASSWORD
#
# 2. 使用本地SQLite：
#    - 设置 DB_TYPE=sqlite（或删除此配置使用默认值）
#    - 无需其他配置
#
# 3. 连接Docker中的PostgreSQL（本地开发）：
#    - 设置 DB_TYPE=postgresql
#    - 设置 DB_HOST=localhost（如果端口已映射到主机）
#    - 或设置 DB_HOST=postgresql（如果在Docker网络中）
#
# =============================================================================

