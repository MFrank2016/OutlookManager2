# ============================================================================
# Outlook邮件管理系统 - 后端依赖
# Python 3.13+ 兼容版本
# ============================================================================

# FastAPI核心
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Pydantic（升级到支持Python 3.13的版本）
pydantic==2.10.6
pydantic-settings==2.7.1
email-validator==2.2.0

# 数据库（仅SQLite，移除PostgreSQL以避免编译问题）
sqlalchemy==2.0.36
alembic==1.14.0
aiosqlite==0.20.0

# 认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# HTTP客户端
httpx==0.28.1

# IMAP客户端
aioimaplib==1.1.0

# 缓存（仅内存缓存，移除Redis）
cachetools==5.5.0

# 日志
structlog==24.4.0
python-json-logger==3.2.1

# 工具
python-dotenv==1.0.1
tenacity==9.0.0

# 测试
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0

# 开发工具（可选）
# black==24.1.1
# flake8==7.0.0
# mypy==1.8.0
# isort==5.13.2

